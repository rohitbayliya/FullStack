// In JS, array is a collection of data types
// let a = [1, 'a', true]

/**
 * Array functions
 * 
 * a[1]
 * a.at(2)
 * a.concat([2, 'b', false])
 * a.pop()
 * a.push('c')
 * a.shift()
 * a.unshift(2,'b')
 * a.includes('1')
 * a.indexOf('a', 1)
 * a.lastIndexOf('a', 1)
 * a.join('')
 * a.length 
 * a.reverse()
 * a.slice(1, 4)
 * a.splice(1, 3)
 * a.splice(1, 0, 23, 45)
 */

/**
 * insert element at specified location
 * ans: array.splice(index, 0, "element")
 */

/**
 * delete element at specified location
 * ans: array.splice(index, 1)
 */

/** take 5 vlaues from user and create an array from them

    let values = [];

    for (let i = 0; i < 5; i++) {
        let input = prompt(`Enter value ${i + 1}:`);
        values.push(input);
    }

    console.log(values);
 * 
 */

/**
 * 
 */